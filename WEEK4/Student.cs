namespace WEEK4;

public class Student
{
    // Instance fields (each object gets its own copy)
    public string name;
    public int age;
    public string address;

    // Static field (shared by all Student objects)
    public static string schoolName = "Global Academy";
}